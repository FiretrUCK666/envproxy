## v1.3.0：候选集合与有序试活（双开 robustness）

- 发现层改交有序候选集（已知顺序加可疑排序加全扫尾巴，全部过握手门），不再只交第一个。
- 状态机记集合签名：变化即走一遍有序试活（上次验活的优先认回，稳态节流单查），认活才认领。
- 双开时前死后活自动让位；单代理、改端口、抖动等老场景行为不变，稳态开销不变。
- 节流缓存按端口分槽（Win 哈希表，Mac 单槽位同契约），多候选下不串味。
- 文档：日常使用加双开行，保护机制加集合试活行。
