## v1.2.0：检查更新与一键升级

新增 `5-检查更新`，升级不用再手动下载覆盖：

- `4-查看状态` 新增三行：本地版本 / 最新版本 / 更新状态；无网络时只提示，不报错。
- `5-检查更新`：查到新版先问你（默认不更新），确认后整包覆盖升级，日志原样保留，不断网。
- Windows 与 macOS 行为一致。

升级方法：进 `win`（Mac 进 `mac`）文件夹，双击 `5-检查更新`，确认后一键完成。
