## v1.4.1：移除仓库内的维护者工具与两条 CI 门禁

- 删掉 `tools/sync-toc.mjs` 以及 README 第八节里对应的条目：仓库回到"零工具"状态——
  除两平台核心脚本与双击壳之外没有任何脚本，CI 也不需要装 Node。
- 跟着撤掉 `check.yml` 与 `release.yml` 里上一版新增的两条 CI 步骤（编码红线、文档目录一致）；
  CI 恢复为只跑 sh 语法与 ps1 语法这两条硬门禁。
- `AGENTS.md`、`CONTRIBUTING.md` 里随之新增的工具与门禁描述一并还原，不留指向已删文件的说法。
- **v1.4.0 的全部行为修正与口径修正不变**：状态机候选集不变量（退出软件约 5 秒删变量）、
  macOS"2-停止监控"真的停住、自启动自愈按内容收敛、Windows 定位器跨搜索根按活跃度排序、
  macOS 卸载清掉 launchd 重定向文件、Windows 兜底探测只读响应头。
- 目录（TOC）与标题的同步改回维护者侧的手动步骤：增删小节或改标题后，由维护者重跑一次
  同步脚本；仓库不再自动校验这件事。
