## v1.2.1：快路径握手门与兼容性补强

- 修复误判：已知端口（如 8080/8888）光在监听不再算候选，必须过 CONNECT 握手——本机开发服务器不会再被误判为代理而错误注入（线上复现验证）。
- 在位复核：已锁定的端口约每 10 轮补一次本机握手（零流量），代理退出后若有别的程序占了同一端口，立即丢缓存。
- 已知端口表新增 Hiddify-Next 默认 12334（只记 HTTP/混合口，不记纯 SOCKS 口）。
- 文档补齐边界：Outline/纯 SOCKS 无本地 HTTP 口（永远发现不了，非 bug）、本地代理认证、WSL2/管理员终端、fish 说明。
