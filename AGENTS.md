# EnvProxy — 项目说明（会话自动注入）

本文件在每次会话开始时自动注入上下文。它是本项目与 AI 之间的接口契约，是项目的唯一
权威约定；任何修改必须遵守本文，并以「构建与验证全绿」为完成标准。

<!-- project-forge:managed -->

<!-- project-forge:kernel:start -->
## 行事总纲（最高准则，优先于一切具体方法）

做任何事，先想清楚「根本」在哪里，再动手。追求一次解决一类问题，而不是反复处理
同一个问题的表象；追求整体清晰、可维护、可演进，而不是越堆越乱。这是所有行动的
最高准则，本文的一切方法都是它的落地方式。

### 四条准则

- **治本，不治标**：遇到问题先找根因，想清楚「怎样才算真正解决」再动手；修一次，
  就要让这一类问题不再复发——只把表面压下去，不算解决；
- **写原则，不写死**：能归为通则的，绝不写成个案特例；能用结构和机制表达的，
  绝不靠写死的细节；不依赖特定细节的做法，环境怎么变都不会失效——通则即稳定；
- **融入结构，不打补丁**：任何新增（能力、规则、流程、工具用法）都要找到它在整体
  中的位置并融入进去，而不是在边上再挂一块；同一个问题第二次出现，说明上次治的
  是标，回头治根；
- **保持清朗，不堆屎山**：所有产物——方案、清单、目录、文件、流程、代码——都要
  结构清晰、职责分明、可维护可演进；宁可多想一步，也不留下一堆难以理解、难以
  修改的堆积物。

### 动手前三问（适用于任何任务）

1. 我这是在治标，还是在治本？
2. 这个做法能覆盖一类问题，还是只覆盖眼前这一个？
3. 做完之后，整体是更清晰了，还是更臃肿了？

### 工程落地纪律（改代码与新增能力时的具体法则）

- **简单成熟，不造轮子**：优先选简单、成熟、被验证过的方案；动手前先问
  「已有的能不能用」，避免过度设计、重复造轮子；
- **先看再动**：修改或新增前，先检查项目现有代码、依赖与文档，摸清现状
  与既有约定，再动手；
- **最小可用，增量扩展**：从最小可用版本起步，按真实需要逐步扩展，不为
  未来需求提前叠加复杂度；
- **模块清晰，职责单一，复用已有**：模块边界清晰、职责单一；优先复用
  已有能力，能并入现成的就不另起炉灶；
- **为长期维护决策**：架构决策考虑长期维护成本，避免只解决眼前问题的
  临时方案——今天的临时补丁，是明天要还的债；
- **小改动，保稳定**：修改前先理解现有实现，尽量小范围改动，确保已有
  功能稳定；能少动就不多动。

任务编排、搜索调研、文件整理……一切具体方法，都是这条总纲的落地方式。

## 本文件的定位与编辑规则

**本文件的地位**：随仓库走，是**项目的契约**，不是某一个人的私有笔记。它描述
「这个项目是什么、要求什么」，而不是「谁在维护它」。这条区分决定了本文能写什么：

| 能写（对任何接手者都成立） | 不能写（随人而变） |
| --- | --- |
| 机制、架构、不变量、硬性规范 | 某个账号的用户名、邮箱、发布作用域 |
| 构建 / 测试 / 发布**该做什么、按什么顺序** | 推送目标、发布目标这些**取值** |
| 为什么这样要求（意图与边界） | 一次性的版本号、日期、某人当时的决定 |

**操作者取值一律现场取**：正文里凡是需要具体值的地方（仓库地址、项目标识、远端、
署名、发布目标），都从**项目自身的权威来源**读取，而不是写在本文里。可用的权威来源
随技术栈不同而不同，常见的几类：

- **清单与元数据文件**：项目标识、版本、依赖、发布范围、入口与产物声明；
- **版本控制配置**：远端地址、当前分支、提交署名（`git remote -v`、
  `git config user.name`、`git config user.email`）；
- **账号登录状态**：当前登录的账号与作用域（各生态的 `whoami` 一类命令）；
- **项目已有的约定文件**：既有文档、既有配置、既有目录结构所体现的事实。

上面只是常见类别，**不是固定清单**——项目里实际存在哪几个，就以哪几个为准；一个都
不存在时，说明该项目还没建立这些事实，那就先建立，再回填本文。**项目是这些事实的
唯一权威来源**：换人、换远端、换发布目标时，改这些真实配置即可，本文不需要跟着改，
也不会失准。

**载体名称不写死**：不同技术栈的清单文件、配置文件名各不相同。本文只写「从哪里取」，
不写「叫什么名字」；需要举例时举例，但举例不是规范——接手者用什么技术栈，就以那个
技术栈的权威来源为准。

**这条只管本文，不管给人看的文档**。README 与 CONTRIBUTING 面向使用者和贡献者，
要能直接复制粘贴，因此**必须写具体地址与命令**；易主时跟着改一次即可（改动集中在
安装命令与反馈渠道几处）。区分标准是**读者是谁**：读者是 AI（需要推理、会读项目）
就现场取值，读者是人（需要照做）就给现成命令。

**别人 fork 之后怎么办**：直接沿用即可。本文的机制与规范描述的是这个项目的做法，
对任何接手者同样成立；而所有「取值」都是现场读的，会自动指向他自己的仓库与账号。
他若想改工作方式（例如换分支模型、换 CI），按下面的编辑规则改本文即可——这正是本文
「是活的」的含义。**因此本文放在仓库里的意义不只是备份**：它是接手者（以及接手者的
AI）理解这个项目的最短路径；哪天仓库只剩本文和代码，也不会有「只有原作者才知道」的
隐性约定。

**本文件是活的**：它随项目演进被更新、增删、重构，不是一份永不变化的快照。会话中
若发现本文与代码现状不一致，以「本文意图 + 代码现状」为准，并按下面规则修正本文。

**AI 有权编辑本文件**。出现以下情况时，AI 应直接更新本文件（并提交到版本控制），
而不是绕过它、只做口头约定、或把特殊处理写死进代码：

- 规则过时或与代码行为不符；
- 用户要求调整项目约定；
- 发现本文无法覆盖的新场景，且该场景会反复出现；
- 本文表述不清、导致理解歧义。

**编辑本文件的小规矩**：

- 面向 AI 阅读：专业、准确、可执行；不写面向终端用户的科普内容（用户要求大白话时，
  在对话中解释，不写进本文件）。
- 与代码一致：修改项目行为后，若本文描述失准，须同步修正；发现过时描述应主动更新。
- 写意图不写快照：优先表达「为什么」与「边界」，让未来会话能推理到未覆盖的情形；
  避免堆砌一次性补丁式的例外条款，避免记录无意义的日期 / 版本号。
- 精炼：不重复、不冗余；结构清晰（标题层级 / 表格 / 代码块）。
- 遵守项目硬性规范（禁 emoji 等）；涉及用户可见能力或命令变化时，同步更新 README。

**文档同步**：README 面向人（能力 / 安装 / 使用 / 命令），本文件面向 AI（机制 / 规范 /
流程），两者描述同一项目。AI 修改项目或本文件后，应检查 README 是否仍准确，
不匹配时同步更新并提交。
<!-- project-forge:kernel:end -->

## 环境与上下文（以实际环境为准，不依赖固定值）

- **运行前提**：Windows 侧要求系统自带 PowerShell（5.1 即可，无需管理员、无需 Node/Python）；
  macOS 侧要求系统自带 `bash/curl/lsof/launchctl`（macOS 12+ 默认 `zsh` 环境验证过，不依赖
  Xcode 附带的旧 `python3`）。具体版本下限以 `README` 面向使用者的声明为准，本文件不复述。
- **项目根**：脚本所在目录即项目根，运行时动态推导（Windows 侧 `%~dp0` / `$PSCommandPath`，
  macOS 侧 `SCRIPT_DIR`），**绝不写死绝对路径、用户名或盘符**——整个文件夹可任意搬家、改名、
  复制备份。重定位范围是有限且明确的：定位器只在「用户目录（桌面/文稿/下载/OneDrive/.config，
  向下 6 层）+ 其他磁盘根（向下 4 层）」里按日志活跃度选新位置，因此备份目录不会被误选；
  搬到这个范围之外（如 `C:\Tools`、`~/Projects`、任意深目录）时，双击一次「安装」接管。
- **可变配置来源**（调参只改这些常量，不改机制）：两核心脚本顶部的已知端口表
  （`$KnownProxyPorts` / `KNOWN_PORTS`）、进程名特征（`$ProxyProcessPatterns` / `PATTERNS`）、
  验证端点表（`$CheckEndpoints` / `CHECK_HOSTS`+`CHECK_PATHS`）、去抖与节流阈值
  （双轮确认、连续失败计数、探测节流间隔，见两脚本监控主循环）。
- **改动生效方式**：改完任一脚本后，进对应平台文件夹（`win\`/`mac\`）双击一次 `1-安装`（万能修复：重建自启动、
  重启监控、加载新代码并当场校正一次变量）。macOS 改完 `mac\envproxy.sh` 须保存为 LF、无 BOM。

## 项目定位

EnvProxy 是一个零依赖的终端代理环境变量自动切换工具：常驻监控每 2–3 秒探测本机
是否存在真实可用的代理端口，三层验证（端口监听 → CONNECT 握手 → 真实节点连通）
确认“真在翻墙”后整套注入代理环境变量（Windows 5 个 / macOS 9 个，见「关键不变量」），
确认断开后整套删除，使用者零操作。

它解决的是“翻墙软件只管浏览器、不管终端工具”的断层：`git/npm/curl` 及各类 CLI
只认环境变量。给同时使用翻墙软件与终端工具的个人用户，Windows 与 macOS 双平台
行为一致（见 `README` 第一节与第十二节的用户视角说明）。

## 架构与关键不变量

### 结构（职责与入口）

| 职责 | 入口文件 | 说明 |
| --- | --- | --- |
| Windows 核心（发现→验证→写入→自启→自愈） | `win\envproxy.ps1` | 参数 `-Install/-Stop/-Uninstall/-Status/-Purge`；无参数进入监控模式。判据细节见文件内分节注释（0.Win32 广播 / 1.端口发现 / 2.用户级变量读写 / 5.日志 / 自启动自愈 / 卸载验尸） |
| macOS 核心（同语义，双路写入） | `mac\envproxy.sh` | `install/stop/uninstall/status` 子命令；无参数进入监控模式。头部注释写明与 Windows 版的差异（`proxy.env` + `launchctl setenv` 双路） |
| 搬家重定位模板 | `mac\locator.sh` | 安装时复制到 `~/.envproxy/locator.sh`；按日志活跃度选新位置并清理旧残留 |
| 在线更新（查 Release→整包覆盖→走安装收尾） | `win\envproxy.ps1 -Update` / `mac\envproxy.sh update` | 状态显示只读（本地/最新/更新状态三行），动手只在更新入口（默认 N 确认、非交互不等待）；先校验后覆盖、跳过 monitor、字节拷贝 |
| 终端入口（逻辑全在核心里） | `mac\install.sh` 等 5 个小 `.sh` | 仅 `cd` 到目录后 `exec` 对应子命令 |
| 双击入口（壳，无逻辑） | `win\` 下 5 个 `.cmd` / `mac\` 下 5 个 `.command` | Windows 壳调 `win\envproxy.ps1` 对应开关；macOS 壳调对应 `.sh`（同目录，搬家必须成组搬） |
| 运行时状态 | `win\monitor\` / `mac\monitor\`（各跟自家核心） | 脚本 `mkdir -p` 自建，**不入库**（见 `.gitignore`）；`stop.flag` 为临时信号，用后即删 |

新增模块时同步更新本表；机制细节以代码注释为准，不复述。

### 双端对等（win 与 mac 是同一产品的两种实现，不是两个产品）

- **判据是行为，不是文件**：两侧代码永远不会逐行一样，对齐的是使用者可感知的行为——
  什么条件下注入/删除、注入哪几个变量、去抖与迟滞语义、日志记什么、自启与自愈语义、
  版本显示与更新确认语义。
  改完能通过这六条对照，即算对齐。
- **行为改动双侧同改**：改判据、阈值语义、变量集合、安装/卸载流程，另一侧必须同步改；
  做不到同时改时，先改一侧并在汇报里记一笔待回齐事项，不许静默单侧上线。
- **单侧 bug 收工前问一句话**：“另一侧有没有同类问题？”——同一判据在另一侧的实现不同，
  但病因可能相同；确认没有再收工，确认结论写进提交说明或汇报一句话。
- **允许有意分叉，但必须显式**：平台差异导致的必然分叉（如写入通道、文件编码），在分叉处
  代码注释标 `DIVERGE(侧): 原因`；分叉消除时删掉标记。不标记的分叉一律视为遗漏。
- **回归对照物**：`README` 第六节故障排查表与第十二节 Mac 差异说明即双端行为基线——改完
  一侧，对着这两处逐行确认另一侧是否仍成立。

### 关键不变量

- **变量整套同进同退**：注入与删除**共用同一张表**，不许只动其中几个——残留单项会造成
  “半代理”诡异行为。变量集合是**平台相关的**，不是“一套名字走两边”：
  - **Windows 侧 5 个**：`HTTP_PROXY/HTTPS_PROXY/ALL_PROXY/NO_PROXY/NODE_USE_ENV_PROXY`；
  - **macOS 侧 9 个**：同上再各加一份小写拼写（`http_proxy/https_proxy/all_proxy/no_proxy`）。
  差异的根据是平台语义，不是实现难易：**Windows 的环境命名空间大小写不敏感**——`HTTP_PROXY`
  与 `http_proxy` 是同一个变量，注册表也只存得下一个；按 curl 惯例“大小写各写一份”在 Windows 上
  达不到目的（只是互相覆盖），还会在运行期留下同名两种拼写的重复环境块，而 .NET 系宿主建
  “大小写不敏感字典”时直接抛 `An item with the same key has already been added`，子进程继续继承
  同一个块，**一个终端窗口里跑的东西会一起中招**。macOS 的 `launchctl` 与环境块真大小写敏感，
  大小写各一份各有归属，故维持 9 个。**不允许在 Windows 侧重引入任何大小写成对的变量名。**
  权威来源：`envproxy.ps1` `$ProxyVarNames` 与 `Get-ProxyVarTable`、`envproxy.sh`
  `set_user_env_vars`；差异在代码处以 `DIVERGE(Win)` / `DIVERGE(Mac)` 标注。
  **新增变量时**：两个平台各自的集合、README 双语变量表与大小写口径说明四处一起改。
  运行时推论（**注册表是干净的，畸形块只在会话里**）：进程环境块在进程创建时定型，
  之后改用户级变量**不会**修复已经存在的进程块。因此修复发布后——
  **新开的终端/程序立刻干净**（由 explorer 拉起，读的是注册表），而修复前就存在的终端
  及其子进程仍带着旧块，要由用户重开那个终端；**不需要注销**。对外口径见 `README` 故障排查。
- **删除清单不得独立于注入清单**：删要按同一张表删（Windows 侧 `$ProxyVarNames`、
  Mac 侧同一份循环清单）。历史上“注入清单”与“`NO_PROXY` 单独删”分家写，导致只写了大写那一次
  留下无主残留——**清单分家必然漂移**。权威来源：`Remove-UserEnvVars` / `remove_user_env_vars`。
- **判“断”必须比判“通”保守**：判“断”的代价是「删变量 + 全系统广播」，影响机器上每一个应用；
  误留代理变量的代价只是多等一会儿。两个代价不对称，判据就不许对称——**单次探测不通 ≠ 代理没了**。
  故迟滞是：连续 3 次探测失败才判“断”（按 15 秒节流约 45 秒），**一次成功立即判“通”并清零计数**
  （重试不累积）。旧值 2（约 30 秒）实测在弱网下产生 60–90 秒一次的状态横跳，每跳一次广播一次。
  这条是行为契约，两侧同改。权威来源：Windows `Test-NodeAlive` 的 `$NodeFailThreshold`、
  Mac `test_node_alive` 的 `NODE_FAIL_THRESHOLD`。
- **`ALL_PROXY` 统一 `http://` 写法**：不许改回 `socks5://`——部分工具（如 dsh）不支持
  SOCKS 写法会警告并跳过，而本地混合端口两种协议都应答，`http://` 兼容面最大。
  权威来源：`README` 变量表下的写法说明。
- **判决三层缺一不可**：监听 → CONNECT 握手（回 `HTTP/1.x 200` 才算代理）→ 真实节点连通；
  不认品牌、不认进程名、不认端口号——进程名表与已知端口表只影响扫描速度，不影响覆盖面
  （全端口 CONNECT 兜底总能发现）。权威来源：`envproxy.ps1` 第 1 节注释、
  `envproxy.sh` 头部“发现来源”注释。
  推论（状态机不变量）：**`on` 只可能来自本轮候选集**——候选集是过了前两道门的端口；
  候选集为空就只能是 `off`，不许沿用上一轮记住的端口（那会给已经消失的端口报 `on`，
  并把调用方的去抖计数反复清零，把“退出软件后秒级删变量”拖成几十秒）。
  权威来源：两核心 `Get-CurrentState` / `get_current_state` 的“集合稳定”分支。
- **去抖与迟滞阈值是行为契约**：状态连续确认多轮才动手、节点连续失败计数达标才判“断”（见上条
  “判‘断’必须比判‘通’保守”）、真实探测每端口 15 秒最多一次且**只等响应头、不读正文**
  （每次不足 1KB，流量预算就靠这两条）。调参只改阈值常量，不许绕过计数、改单次判定。
  Windows 侧阈值为函数内具名常量（`$NodeCheckThrottleSec` / `$NodeFailThreshold`），
  Mac 侧为脚本头部具名常量（`NODE_CHECK_THROTTLE_SEC` / `NODE_FAIL_THRESHOLD`）——
  两侧都不许再出现裸数字。权威来源：两脚本监控主循环
  （ps 侧按端口分槽的 `NodeState` 计次器 + `ResponseHeadersRead`，Mac 侧单槽位同契约）。
- **off 不等于零外网**：只要本机还有能应答 CONNECT 握手的端口（典型是“断开连接但内核还活着”），
  节点探测就照常进行——那是区分“断开”与“翻墙中”的唯一手段。零外网流量只在
  “没有任何端口在应答握手”时成立。对外口径见 `README` 第七节。
- **只碰用户级环境变量，不碰系统代理**：Windows 写 `HKCU\Environment` + 广播
  （`SendMessageTimeout`），macOS 写 `~/.envproxy/proxy.env` + `launchctl setenv`；
  不写系统目录、不要求管理员。卸载（`3-一键恢复`/`uninstall --purge` 之外默认保留日志）
  必须先杀监控再清自启动，否则下一个自查周期（每 30 轮，约 60–90 秒）自愈会把自启动重建回来
  （见 ps 侧“卸载验尸”注释）。
  权威来源：`envproxy.ps1` 第 2 节与自启动节、`envproxy.sh` 双路写入与 hook 标记。
- **自启动 = 登录时拉起一次，不保活**：Windows 侧是 Run 键，macOS 侧是 LaunchAgent 的
  `RunAtLoad`——两边都刻意**不用**“常驻保活”语义（Mac 侧 plist 不写 `KeepAlive`）：
  保活会让“2-停止监控”被系统自动撤销（约 10 秒后又把监控拉回来、变量重新注入），
  还会在“监控已由 `nohup` 启动”时形成每 10 秒一次的无谓 fork。
  因此“停止”是一个稳定状态，直到下次登录或用户再点一次“安装”。
  自愈的判据是**内容与当前代码一致**（不只是文件还在）：升级后没走“安装”也不会留下旧版定位器。
  权威来源：ps 侧 `Repair-AutoRun`/`Test-LauncherCurrent`、Mac 侧 `repair_autorun`/`get_plist_content`。
- **编码三条红线**（见硬性规范）：`envproxy.ps1` 必须 UTF-8 **带 BOM**
  （PowerShell 5.1 在中文系统按 ANSI 读无 BOM 文件会语法报错），`envproxy.sh` 必须 LF
  **无 BOM**（BOM 会顶掉 shebang），仓库统一 LF 入库（`.gitattributes`）。
- **`win\monitor\` 与 `mac\monitor\` 永不入库**：`monitor.pid`/`monitor.log`/`stop.flag`
  是本机运行时状态，各跟自家核心、脚本自建；跨位置停止信号必须写到**目标自己的目录**
  （优雅退出而非强杀）。布局约束：monitor 必须与核心同目录——两平台定位器都按
  “标记文件名搜候选、同级 monitor.log 排活跃度”工作，搬走任一边都会破坏定位。
  权威来源：`.gitignore`、两核心 `mkdir -p` 与跨位置停止函数。
- **状态显示只读、更新显式确认**：状态函数只读版本信息（查不到只提示），永不写文件、
  永不提问；升级只走更新入口，发现新版默认 `N`（回车=不更新），非交互环境直接给提示退出。
  权威来源：两核心更新节与状态函数。
- **更新先校验后覆盖、日志永不丢**：下载标签源码包整包覆盖（防漏文件）；包内 `VERSION`
  与目标一致且核心文件存在才动手，否则中止且现版不动；覆盖跳过 `win\monitor\`/
  `mac\monitor\`（字节拷贝保编码，只覆盖不删除）。更新源有 `.git` 时从 git remote 解析，
  否则用与 README 克隆地址同源的内置默认。权威来源：两核心更新节。
- **快路径不认端口号、只认握手**：已知端口在监听还要过 CONNECT 才算候选
  （本机开发服务器不误判）；已锁定端口约每 10 轮补一次本机握手复核（零流量），
  失守立即丢缓存（照常进双轮去抖）。已知端口表只是“常见入站口”的提速提示：按常见入站口收、
  不按协议收（列入纯 SOCKS 口不会误判，只会多一次本机握手——判决永远走握手门），
  追加有来源的默认值，不写死分支。状态机记候选集合签名：变化即走一遍有序试活
  （上次验活的优先认回，稳态节流单查），认活才认领。权威来源：两核心发现节与端口表。

## 构建与验证（改完必跑，全绿才算完成）

本项目**无构建、无测试框架**（单文件脚本，零依赖）：下面即全部验证手段。

| # | 命令（仓库根下执行） | 在验证什么 | 门禁 |
| --- | --- | --- | --- |
| 1 | `bash -n mac/envproxy.sh mac/locator.sh mac/install.sh mac/stop.sh mac/uninstall.sh mac/status.sh mac/update.sh`（macOS/Git-Bash；CI 同款） | sh 语法可解析 | 硬门禁 |
| 2 | PowerShell 语法解析 `win\envproxy.ps1`（本机 `powershell` 跑 `Parser::ParseFile`；CI 同款） | ps1 无语法错误、中文无乱码（BOM 完好；必须用语法解析而非仅分词——BOM 损坏报的是解析错误） | 硬门禁 |
| 3 | Windows 进 `win` 双击一次 `4-查看状态`（或 `bash mac/status.sh`） | 状态/变量/最近日志与预期一致 | 硬门禁 |
| 4 | 改完核心逻辑后进 `win` 双击一次 `1-安装` 重载，再看一轮状态翻转 | 新代码实际被监控进程加载、注入/删除行为正确 | 建议（改核心必做） |

`win\monitor\`（或 `mac\monitor\`）状态、已开旧终端窗口不作为验证依据（旧终端不继承新变量是预期行为，
见 `README` 故障排查）。CI（`check.yml` 与 `release.yml` 内嵌的 check job）跑的即第 1–2 条，
与本地同一批——改验证手段时三处一起改。

## 版本与发版

**先确认角色**（涉及推送或发版前做一次）：`git remote get-url origin` 指向正本
（`https://github.com/FiretrUCK666/envproxy.git`，以 README 克隆地址为准）= 维护者，
走下面全部流程；指向自己的 fork 或没有远端 = 贡献者——改代码、跑门禁照做，
但到此为止：推自己的分支并开改动请求，**不推标签、不建 Release**。请求事件天然拿不到
Secrets，需要钥匙的步骤不会跑也不会泄。判断不了时按贡献者走。

- **版本号唯一权威源**：根目录 `VERSION` 文件（纯文本 `x.y.z`，如 `1.0.0`）。
  打标签、写文档、回答用户，版本号一律从它读，不在别处各写一份。
- **抬号判据**：使用者可见变化才抬（行为、交互、报错文案、随包分发的中英文 `README`、
  兼容性声明）；重构、测试、构建脚本、不进包的内部文档不抬。日常攒提交时抬，
  发版只打标签——标签是里程碑，不是提交的附庸。
- **标签形状**：`v<VERSION>`（如 `v1.0.0`）。CI 自动校验，不一致拒绝发布。
- **发版动作只有一个**：`git push origin vX.Y.Z`。`release` job 自动接管：
  先跑门禁 check → 校验标签与 `VERSION` 一致 → `gh release create --generate-notes`
 （幂等：Release 已存在则跳过）。日常推送分支**不触发**发版。
- **Release 正文默认自动生成**（英文，来自提交记录；提交信息保持英文ASCII即无
  非ASCII经shell风险）。要中文正文时：写 UTF-8 文件由脚本按字节发送、发送后回读
  比对，不经 shell 拼接（见 skill 的 `scripts/release-notes.mjs` 用法）。
- **中文正文随版一起走**：发版前把 `release-notes/<标签>.md` 写好一起提交，
  workflow 有文件就用文件发布，没有才回退英文自动生成。文件是 UTF-8 落盘、从磁盘读，
  中文不经 shell；内容在 PR 里可评审。不新增任何钥匙。
- **`RELEASE_TOKEN` 运维**：仓库 Secrets 里 exact 同名，细粒度、仅 Contents 读写、
  仅本仓库、有过期时间。过期或换人**只换 Secrets 里那一个值**，工作流文件不动；
  到期前换发是维护者的周期事项，发布前先确认它有效。
- **补漏**：标签已推但 Release 没建（job 失败、中断），用 `workflow_dispatch`
  手动触发并填标签名重跑——**不重打标签**（版本号永不复用）。
- **已发布的版本只用新版本修**：废弃用标记，不删除（别人的锁定引用可能已写死该号）。

## 硬性规范

以下几条对任何项目都成立，是底线：

1. **提交信息与文档不得包含 emoji**。
2. **命名一致性**：同一概念在所有位置使用同一个名字，不引入同义变体。
3. **不新增依赖**：需要新依赖时先说明理由并确认，不擅自引入。
4. **改完必须过构建与验证**（见上），不提交未验证的改动。
5. **文档与代码同步**：行为变了就同步文档，发现文档失准主动修正。

本项目特有的硬性规范（每条都是出过或挡住过真实事故的）：

6. **`envproxy.ps1` 永远 UTF-8 带 BOM 保存**：无 BOM 时中文系统 PowerShell 5.1 按 ANSI
   读取，中文注释撑破语法结构直接报“意外的标记”（见 `README` 故障排查）。
7. **`envproxy.sh` 永远 LF、无 BOM 保存**：BOM 会顶掉 shebang 导致无法执行；与上一条方向
   相反，改一侧时不许顺手“统一”另一侧（见 `README` 12.6）。
8. **只用系统自带命令，不引入任何运行时依赖**：零依赖是安装口号（“不需要装任何东西”），
   引入 Python/Node/第三方模块即破坏产品承诺（见两核心脚本头部注释）。
9. **不写死端口、进程名、路径**：新增适配只向已知端口表/进程名特征**追加**，不许把某次
   抓到的端口写成默认分支条件——判决永远走三层验证。
10. **包装壳不许长逻辑**：`.cmd`/`.command`/小 `.sh` 只做“进目录 + 调核心”，行为改动只进
    `envproxy.ps1`/`envproxy.sh`，不许在壳里分叉。
11. **中英文 README 节一一对应、同步改**：`README.md` 与 `README.en.md` 节数量、
    节顺序、节内事实/命令/地址必须一致；改任一侧必须同步另一侧，改完重跑目录同步脚本，
    汇报时说明另一份是否同步。英文版用自然英文表达（不逐字硬译），但不许漏事实、
    不许多承诺。目录与徽章是派生内容，只用脚本生成与校验，不手工维护。

## 版本管理（必守）

本项目**不发布到任何包生态**（npm / PyPI / crates.io 一类都没有制品）：对外分发只有
GitHub Release 一条路，见「版本与发版」。因此这里不涉及包版本号语义、发布作用域、
制品签名这些事项，版本管理的日常范围就是本地提交、推送与标签。

<!--
本节按「是否要发布」分成两类，不要因为其中一段用不上就删整节：
- **永远适用**：提交纪律、回滚分档、不改写已推送历史——与发不发布无关；
- **要发布时适用**：版本号语义、抬版本号判据、发版规则。
-->

本项目是「本地仓库 + 远端」结构，两处**互不自动同步**：推送之后远端才更新。

### 日常闭环

- 确认工作区状态；有未提交改动，先存一个可回退的存档点。
- 改代码。
- 跑构建与验证，全绿。

- 提交，然后推送。

**改动量大或有风险的操作之前，先提交当前状态**，保证永远有一个可回退的干净起点。

### 提交纪律

提交信息简短说明这次改了什么，中文或英文都行但保持一致，**禁止 emoji**；一次实质变更
一个提交，不要把无关改动混在一起。**产物与产生它的源改动必须同一次提交**，否则历史里
会留下产物与源码不一致的中间状态。

### 回滚分档

| 状态 | 手段 |
| --- | --- |
| 未提交 | 丢弃工作区改动 |
| 已提交、未推送 | 回退到存档点 |
| 已推送 | 用反向提交——**不改写已推送的历史** |

**不改写已推送的历史**：不用强制推送、不对已推送的提交做变基——别人可能已经基于它工作。
这条在发布过的项目上尤其重要：历史一旦公开，任何人都可能已经基于它建了分支。

## 测试（约定）

本项目**无自动化测试、无测试文件**（明确写无，不是不写）：验证靠“构建与验证”节的
四条手段，其中第 3–4 条是事实上的冒烟测试。

- **回归依据**：`README` 第六节故障排查表即回归清单——改完探测/写入逻辑后，逐行确认
  表中现象与原因描述仍然成立（尤其是“断开≠退出”“变量反复横跳”两条最易被改坏的）。
- **新增探测逻辑的约定**：必须同时走“快路径已知端口 + 万能路径全端口 CONNECT 兜底”
  两层验证，不许只加一种；验证端点新增必须**同时**满足两条：跨厂商/跨域段（现有 7 端点
  分组见两脚本端点表注释）、响应体“零字节或极小”（Mac 侧探针把正文收下再丢弃，
  正文字节数直接等于流量预算）。两条缺一不可，否则“每次不足 1KB”的流量口径立刻失真。
- 清单以目录扫描为准：若将来新增测试脚本，其位置与命名以目录现状为准，不在本节
  维护文件列表。

## 文档同步

本项目有两类面向不同读者的文档，**描述同一件事，但读者不同**：

| 文档 | 读者 | 因此 |
| --- | --- | --- |
| 本文件 | AI 助手 | 写机制与判据；需要具体值时写「从哪里取」，让它自己去读项目 |
| 面向人的文档（`README` 等） | 使用者 / 贡献者 | 写具体、可直接复制的命令与地址 |

AI 修改项目或本文件后，应检查面向人的文档是否仍然准确，不匹配时同步更新并提交。
判据是**读者是谁**：读者是 AI 就现场取值，读者是人就给现成命令。

### 派生内容用工具同步，不手工维护

有一类内容**完全由别处的信息决定**：目录由标题决定、徽章由外部服务决定、依赖清单由
依赖声明决定、模块表由实现的接口决定。它们不该手工维护——手工维护的派生内容一定会
漂移（加了一节忘了加目录项、改了标题忘了改锚点），而这类错误的症状很隐蔽：
**锚点写错的表现是「点了没反应」，不报错、不显眼**，作者也不会去点自己的目录。

判据一句话：**这份内容能不能由一条命令从源头重新算出来？** 能，它就不该手写。

做法：用脚本从源头生成，插在成对标记之间（标记之外一个字节不动），改完重跑即同步；
并在持续集成里加一条「重新生成后与提交一致」的检查——这样漂移会当场暴露，而不是
等到某个读者点了没反应。

与「本文件的定位与编辑规则」里那条「写意图不写快照」是同一个道理：**快照会过期，
派生关系不会**——把派生关系交给工具，剩下的只有源头需要维护。

### 双语说明文档要**成对**维护

本项目的说明文档有多个语言版本（见项目根目录里的那几份 `README*.md`）。它们不是
「两份文档」，而是**同一份文档的两个版本**，因此必须成对维护：

- **两份都随包发出、都展示在制品库页面上**——改任何一份都算用户可见变化，
  都要按上面的规则抬版本号。只改一份却不抬，会出现「仓库里改了、页面上还是旧的」。
- **漂移是这类文档的主要失效方式**：一次改动只更新了一份，两份说着不同的事，
  而读者不知道自己看的那份是不是最新的。**所以改一份时必须打开另一份一起看。**
- **默认语言那份是权威**，其余是它的翻译。两者不一致时以权威那份为准，并同步另一份。
- **不要机械对译**：不同语言的读者关心的问题不同（安装方式、可用平台、常见报错
  都可能不一样），逐句翻译会产出一份读起来像翻译腔、又没回答本地读者问题的文档。
- **不要在没人的地方新增第三种语言**：一份没人维护的语言版本比没有更糟——它会给
  读者一个过期的印象，而且没人会去核对。

**改完要能回答一句话**：「另一份同步了吗？」——同步了就说改了哪几处；没同步就说
清为什么（例如那条内容只对其中一种语言的读者有意义）。**答不上来，就是漏了。**

